name: paystack_bottomsheet
description: "A Flutter package that opens flutter_paystack_plus inside a 
  bottom sheet modal for a seamless in-app payment experience."
version: 0.0.1
homepage: https://github.com/shegzzy/flutter_paystack_modal

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_paystack_plus: ^2.5.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# The following section is specific to Flutter packages.
flutter:
